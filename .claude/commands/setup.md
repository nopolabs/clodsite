Run the **Setup** workflow documented in `AGENTS.md`.

Argument forms: `/setup`, `/setup clean`, `/setup clean <site-name>`.

Follow `AGENTS.md` for all behavior — credential collection, the permissions the
token needs, the script sequence, and the secret-handling rules (never echo a
full token; mask when confirming).
