Run the **Build** workflow documented in `AGENTS.md`.

Argument form: `/build <site-name>`. If no site name was given, ask for one and
stop.

Follow `AGENTS.md` for the validate → render → Eleventy → headers script
sequence and error handling.
