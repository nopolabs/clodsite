Run the **Teardown** workflow documented in `AGENTS.md`.

Argument forms: `/teardown <site-name>`, `/teardown <site-name> clean`. If no
site name was given, ask for one and stop.

Follow `AGENTS.md` for the destruction summary, the type-to-confirm safety gate,
the teardown/clean scripts, and output interpretation.
