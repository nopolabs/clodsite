Run the **Domain** workflow documented in `AGENTS.md`.

Argument form: `/domain <site-name>`. If no site name was given, ask for one and
stop.

Follow `AGENTS.md` for reading/updating `custom_domain`, running `domain.sh`, and
interpreting the CNAME outcomes and common errors.
