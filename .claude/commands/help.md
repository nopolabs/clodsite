Show Clodsite quick-start help.

**[LLM]** Display the **Quick Start** table from `AGENTS.md` (Setup → create
`build-plan.yaml` → Build → Deploy), the local-preview note, the `SITES_DIR`
note, and the requirements (Node 18+, Wrangler, Cloudflare account). Then point
the user at `AGENTS.md` for the full workflow reference.
