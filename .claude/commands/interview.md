Run the **Interview** workflow documented in `AGENTS.md`.

Argument form: `/interview <site-name>`. If no site name was given, ask for one
and stop.

The interview is the optional guided path to a valid `build-plan.yaml`. Follow
`AGENTS.md` for the question flow, the component-catalog step, the
confirm-before-writing rule, the plan schema, and validation.
